# ---- Compiled binaries ----------------------------------------------------
# Produced by `go build` / `make build`. Never commit.
/davinci-dkg-node
/dkg-runner
/circuit-compile
/protocol-vectors
*.exe
*.test
*.out

# ---- Circuit artifacts ----------------------------------------------------
# Cached proving/verifying keys and constraint systems. Regenerated locally
# on first run via `make circuits-compile`.
/artifacts/
/tmp/

# ---- Foundry --------------------------------------------------------------
solidity/out/
solidity/cache/
solidity/cache_forge/
solidity/broadcast/
solidity/deploy.log
solidity/.last_deployed_addresses.env

# ---- Go -------------------------------------------------------------------
vendor/
*.prof
*.coverprofile
coverage.txt
coverage.html

# ---- Editor / OS ----------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---- Secrets --------------------------------------------------------------
.env
.env.local
*.pem
*.key
!solidity/src/**/*.key

# ---- Local state ----------------------------------------------------------
.davinci/
data/
# …but the explorer's data layer is source, not local state: the unanchored
# `data/` above would otherwise silently exclude the whole hooks package.
!ui/src/data/
logs/
.claude/

# ---- Working / draft documents (not for the public repo) ----------------
/ANALYSIS_*.md
/CIRCUITS_AUDIT*.md
/PLAN.md
/PLAN_PHASES.md
/REVIEW*.md
/SECURITY.md
/ORIGINAL_PAPER.tex
/paper.tex
/paper_epochs_v1.tex
/references_epoch.bib
/vocdoni-app/

# ---- Operator secrets dropped in the working tree ------------------------
/sepolia_privkey.txt

# ---- pnpm content-addressable store --------------------------------------
# Created at the repo root when pnpm runs in a container whose $HOME lives on
# another filesystem (see the Docker invocations in ui/README.md).
/.pnpm-store/
