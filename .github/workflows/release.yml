name: Release

# Triggered when a semver tag (e.g. v0.1.0, v1.2.3, v1.2.3-rc1) is pushed.
# Builds fully-static Linux binaries with the version string baked in,
# publishes the node + UI Docker images, and attaches the binary tarballs
# + checksums to a GitHub Release tied to the tag.

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. v0.1.0). Must already exist."
        required: true
        type: string

permissions:
  contents: write

jobs:
  # ---------------------------------------------------------------------------
  # 1. Cross-compile static binaries for every target in the matrix. The
  #    Go binary is UI-blind, so this stage has no Node/pnpm dependency.
  # ---------------------------------------------------------------------------
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux-amd64
            runner: ubuntu-latest
            goos: linux
            goarch: amd64
            cgo: "0"
          - target: linux-arm64
            runner: ubuntu-latest
            goos: linux
            goarch: arm64
            cgo: "0"
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Resolve version
        id: version
        shell: bash
        run: |
          if [ -n "${{ github.event.inputs.tag }}" ]; then
            echo "version=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "version=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build davinci-dkg-node
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: ${{ matrix.cgo }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          mkdir -p dist/${{ matrix.target }}
          go build -trimpath \
            -ldflags "-s -w -X github.com/vocdoni/davinci-dkg/internal/version.Version=${VERSION}" \
            -o dist/${{ matrix.target }}/davinci-dkg-node \
            ./cmd/davinci-dkg-node

      - name: Build dkg-runner
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: ${{ matrix.cgo }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          go build -trimpath \
            -ldflags "-s -w -X github.com/vocdoni/davinci-dkg/internal/version.Version=${VERSION}" \
            -o dist/${{ matrix.target }}/dkg-runner \
            ./cmd/dkg-runner

      - name: Package tarball
        id: package
        shell: bash
        env:
          VERSION: ${{ steps.version.outputs.version }}
          TARGET: ${{ matrix.target }}
        run: |
          pkg_dir="davinci-dkg-${VERSION}-${TARGET}"
          mkdir -p "release/${pkg_dir}"
          cp dist/${TARGET}/davinci-dkg-node "release/${pkg_dir}/"
          cp dist/${TARGET}/dkg-runner      "release/${pkg_dir}/"
          cp README.md LICENSE .env.example "release/${pkg_dir}/"
          (cd release && tar -czf "${pkg_dir}.tar.gz" "${pkg_dir}")
          (cd release && sha256sum "${pkg_dir}.tar.gz" > "${pkg_dir}.tar.gz.sha256")
          echo "tarball=release/${pkg_dir}.tar.gz" >> "$GITHUB_OUTPUT"
          echo "checksum=release/${pkg_dir}.tar.gz.sha256" >> "$GITHUB_OUTPUT"

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.target }}
          path: |
            ${{ steps.package.outputs.tarball }}
            ${{ steps.package.outputs.checksum }}
          retention-days: 7

  # ---------------------------------------------------------------------------
  # 2. Publish the node Docker image for the tag (and refresh the `latest`
  #    alias so Watchtower-based deployments pick up the new release).
  # ---------------------------------------------------------------------------
  docker:
    name: Docker
    needs: build
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/docker-build.yml
    secrets: inherit
    with:
      image-tag: ${{ github.event.inputs.tag || github.ref_name }}
      push: true

  # ---------------------------------------------------------------------------
  # 3. Publish the standalone UI image for the same tag. Independent of the
  #    node image — the UI ships separately so deployments can pin them at
  #    different versions when needed.
  # ---------------------------------------------------------------------------
  ui-docker:
    name: UI Docker
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/ui-docker-build.yml
    secrets: inherit
    with:
      image-tag: ${{ github.event.inputs.tag || github.ref_name }}
      push: true

  # ---------------------------------------------------------------------------
  # 4. Collect every matrix tarball + checksum and publish the GitHub Release.
  # ---------------------------------------------------------------------------
  publish:
    name: Publish GitHub release
    needs: [build, docker, ui-docker]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download all release artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: release-assets
          merge-multiple: true

      - name: Aggregate checksums
        working-directory: release-assets
        run: |
          cat *.sha256 > SHA256SUMS
          cat SHA256SUMS

      - name: Resolve tag
        id: tag
        shell: bash
        run: |
          if [ -n "${{ github.event.inputs.tag }}" ]; then
            echo "tag=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: ${{ steps.tag.outputs.tag }}
          draft: false
          prerelease: ${{ contains(steps.tag.outputs.tag, '-') }}
          generate_release_notes: true
          files: |
            release-assets/*.tar.gz
            release-assets/*.sha256
            release-assets/SHA256SUMS
